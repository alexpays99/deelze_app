import 'package:flutter/material.dart';

class DeelzeScreen extends StatelessWidget {
  const DeelzeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Deelze Screen'),
    );
  }
}
